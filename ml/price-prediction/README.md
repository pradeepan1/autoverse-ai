# Price Prediction — ML Module

Structure:

- `training-pipeline/` — batch training pipeline (to be implemented)
- `inference-service/` — FastAPI inference service skeleton (bootstrap only, no model logic yet)

No models, feature engineering, or training code are included at this stage, per project scope.
