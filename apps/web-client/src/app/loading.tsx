"use client";

/**
 * AutoVerse AI — Next.js Loading Page (Route Suspense Boundary)
 *
 * Rendered automatically by Next.js during route transitions.
 * Per docs/Architecture.md (Frontend — route-level code splitting).
 */

import { LoadingPage } from "@/components/feedback/LoadingPage";

export default function Loading() {
  return <LoadingPage />;
}
