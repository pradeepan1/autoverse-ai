# Database

Structure:

- `migrations/` — Alembic migration scripts (none yet — no tables defined at this stage)
- `seeds/` — Seed data scripts (none yet)

Table schemas, migrations, and seed data are introduced in a subsequent development phase, per project scope and `docs/PROJECT_RULES.md`.
