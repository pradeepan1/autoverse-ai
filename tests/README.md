# Tests

Structure:

- `unit/` — Unit tests for business logic and utility functions
- `integration/` — Integration tests across services, database, and external dependencies
- `e2e/` — End-to-end tests simulating full user journeys

No test cases are included at this stage — this is the structural foundation only, per project scope. Tests are added alongside each feature as it is implemented.
